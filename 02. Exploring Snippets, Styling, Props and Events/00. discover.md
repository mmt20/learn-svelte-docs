# Section 2 : Exploring Snippets, Styling, Props and Events

## Component Communication & Events
- **Props**: Using the `$props()` rune to receive data from parents.
- **Destructuring & Defaults**: Efficiently handling and defaulting props.
- **Event Attributes**: Using `onclick`, `oninput`, etc., with Svelte 5.
- **Custom Events**: Passing functions as props for component communication.

## Styling in Svelte
- **Scoped CSS**: Understanding isolation in Svelte components.
- **Advanced Styling**: Using SASS, `:global()`, and the `class:` directive.
- **clsx**: Combining classes dynamically with `$derived`.

## Advanced Patterns
- **Deep Dive**: Complex render tag patterns and type-safe snippets.
- **Programmatic Interaction**: Using component exports, `bind:this`, and `svelte:element`.
